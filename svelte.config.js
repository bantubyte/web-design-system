// Minimal Svelte config used by @sveltejs/package and svelte2tsx.
// We don't ship a SvelteKit app — this exists purely to enable
// `npm run build:svelte` and IDE tooling to load Svelte 5 settings.
const config = {
	compilerOptions: {
		runes: true,
	},
};

export default config;
